"""Native-MLX port of the DSpark speculative-decoding stack (Apple Silicon).

Sibling to the PyTorch `deepspec/` package, which stays as the parity oracle.
See the approved plan and deepspec_mlx/spikes/ for the M1 de-risk work.
"""
