# MLX runtime for the DSpark port (torch-free by design — see the plan).
# Pin exact versions once M1 confirms the mlx-lm cache/spec-decode API, since
# mlx-lm's cache surface has churned across releases. Start unpinned to grab the
# latest wheels, then freeze with `pip freeze > requirements.lock.txt`.
mlx
mlx-lm
# transformers 5.13.0 broke mlx-lm 0.31.3's AutoTokenizer.register("NewlineTokenizer",
# ...) call (register() now requires a class, not a str). Pin to the 5.10.x line
# (also what the torch reference repo uses) until mlx-lm catches up.
transformers==5.10.2
numpy
safetensors
huggingface_hub
