# Copy this file to `.env` and fill in your token. `.env` is gitignored.
# Load it into the container with: docker run --env-file .env ...
# Docker's --env-file wants bare KEY=value lines (no quotes, no `export`).
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx
